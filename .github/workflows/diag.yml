name: Diag (one-off, delete after use)

on:
  workflow_dispatch: {}

jobs:
  diag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: |
          pip install -r analysis/requirements.txt
          pip install -r requirements.txt

      - name: Install flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Start DB proxy
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_DB_API_TOKEN }}
        run: |
          nohup flyctl proxy 15432:5432 -a top10-active-etf-db > proxy.log 2>&1 &
          echo $! > proxy.pid
          for i in $(seq 1 30); do
            if (echo > /dev/tcp/127.0.0.1/15432) 2>/dev/null; then
              echo "proxy ready after ${i}s"
              break
            fi
            sleep 1
          done

      - name: Clean up the two future-dated capitalfund rows
        env:
          DATABASE_URL_RAW: ${{ secrets.DATABASE_URL }}
        run: |
          export DATABASE_URL=$(python -c "import os,re; s=os.environ['DATABASE_URL_RAW'].strip().lstrip('﻿'); print(re.sub(r'@[^/]+/', '@localhost:15432/', s))")
          python analysis/_diag_fix_future_dates.py

      - name: Generate top-movers report (preview only, no commit)
        env:
          DATABASE_URL_RAW: ${{ secrets.DATABASE_URL }}
        run: |
          export DATABASE_URL=$(python -c "import os,re; s=os.environ['DATABASE_URL_RAW'].strip().lstrip('﻿'); print(re.sub(r'@[^/]+/', '@localhost:15432/', s))")
          python analysis/generate_top_movers_report.py

      - name: Stop DB proxy
        if: always()
        run: kill "$(cat proxy.pid)" 2>/dev/null || true

      - uses: actions/upload-artifact@v4
        with:
          name: preview
          path: |
            docs/top_movers.html
            ntfy_payload.json
